# Vercel's Python runtime installs from this file, not pyproject.toml.
#
# Deliberately trimmed against the dev environment, because the serverless bundle
# has a 250MB uncompressed ceiling:
#   scipy       71MB — imported nowhere in the codebase
#   supabase-py       — never imported; the store talks to Postgres via psycopg
#   uvicorn           — Vercel runs the ASGI app itself
# That takes the bundle from ~316MB to well under the limit.

fastapi>=0.115
pydantic>=2.9
# Auth. argon2-cffi is ~1MB with its bindings and python-multipart is ~50KB, so neither
# threatens the 250MB ceiling the trimming above exists to stay under.
argon2-cffi>=25.1
python-multipart>=0.0.32
python-dotenv>=1.0
httpx>=0.27
psycopg[binary]>=3.2
openai>=1.54
tavily-python>=0.5
pdfplumber>=0.11
networkx>=3.4
numpy>=2.1
rapidfuzz>=3.10
unidecode>=1.3
